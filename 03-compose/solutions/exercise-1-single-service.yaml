# Solution - Exercise 3.1 Single Service
# Run:  docker compose -f 03-compose/solutions/exercise-1-single-service.yaml up --build
# (put a Dockerfile at the repo root first, e.g. cp 02-dockerfiles/solutions/Dockerfile.exercise-1 Dockerfile)

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      APP_ENV: production
